extends base
block content
  include search
  section.signup
    h2 UPDATE PLANS
    .signup-parent
      for plan in plans
        include upPlanCard
      a(href='/addPlan').add.signup-child
        i.fa.fa-plus-circle(aria-hidden='true')
        #text Add Plan